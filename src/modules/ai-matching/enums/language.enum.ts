/**
 * Supported languages/locales for the AI Matching module.
 * Controls prompt template selection and embedding model routing.
 */
export enum Language {
  /** English. */
  EN = 'en',

  /** Amharic. */
  AM = 'am',
}
