/**
 * A locale code for the AI Matching module.
 * Open string type to support future languages beyond en/am.
 * Validation happens in the configuration / DTO layer, not here.
 */
export type LocaleCode = string;
