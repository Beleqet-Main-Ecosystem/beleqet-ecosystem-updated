import { PrismaService } from '../../prisma/prisma.service';
import { AiChatProvider } from '../resume-brain/ai/ai-chat-provider.interface';
import { GenerateQuestionsDto } from './dto/generate-questions.dto';
import { SubmitAnswersDto } from './dto/submit-answers.dto';
import { GenerateQuestionsResult, SubmitAnswersResult } from './interfaces/skill-tester.interfaces';
export declare class SmartSkillTesterService {
    private readonly prisma;
    private readonly ai;
    private readonly logger;
    constructor(prisma: PrismaService, ai: AiChatProvider);
    generateSession(userId: string, dto: GenerateQuestionsDto): Promise<GenerateQuestionsResult>;
    generateQuestions(userId: string, dto: GenerateQuestionsDto): Promise<GenerateQuestionsResult>;
    submitAnswers(userId: string, dto: SubmitAnswersDto): Promise<SubmitAnswersResult>;
    private assertAuthenticatedUserId;
    private assertGeneratePayload;
    private assertSubmitPayload;
    private requestQuestionsFromAi;
    private buildUserPrompt;
    private sanitizeJobRoleForPrompt;
    private parseAndValidateQuestions;
    private sanitizeAiJsonResponse;
    private parseJson;
    private firstJsonObject;
    private firstJsonArray;
    private firstBalanced;
    private toValidatedOptions;
    private asNonEmptyString;
    private toStringArray;
    private aiGenerationFailed;
    private toHttpException;
}
